cayuga
athena
masonlab
weill cornell
jak4013
ohpc
phobos
cac\.cornell\.edu
WCM
scu@med
scu-cpu
scu-gpu
scu-login

# API-key / token shapes — catches Anthropic keys + OAuth tokens
# in their actual form (require ≥6 digits OR alphanumerics after the
# `sk-ant-api03-` prefix, to avoid false-positive matches against the
# documentation strings that *describe* the patterns themselves).
# Added 2026-04-23 after review handoff finding #11.
sk-ant-api[0-9]{2}-[A-Za-z0-9]{6}
sk-ant-oat[0-9]{2}-[A-Za-z0-9]{6}
Bearer sk-ant-[A-Za-z0-9]{6}
